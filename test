#include<iostream>
using namespace std;

int main()
{
    int a, b;
    cin >> a >> b;
    if (a > 0)
        if(b > 0)
            cout << "duong";
        else
            cout << "am";
    else
        if(b > 0)
            cout << "am";
        else
            cout << "duong";
}
